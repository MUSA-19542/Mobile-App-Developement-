<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#000000" /> <!-- Background color set to black -->
    <stroke
        android:width="4dp"
        android:color="@color/orange" /> <!-- Border color set to darkgolden with 2dp width -->
    <corners android:radius="20dp" /> <!-- Adjust this radius to change the roundness of corners -->
</shape>
